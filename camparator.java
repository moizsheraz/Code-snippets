Arrays.sort(pairs, (a, b) -> a[1] - b[1]);
// sort array on the basis of second element of each pair (in ascending order)

// sort  the 2D Array on the basis of first index

Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0])); 
